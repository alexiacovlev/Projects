CREATE TABLE [dbo].[CIMS_d_Subjects] (
    [ID]   INT            IDENTITY (1, 1) NOT NULL,
    [Name] NVARCHAR (200) NOT NULL,
    CONSTRAINT [PK_CIMS_d_Subjects] PRIMARY KEY CLUSTERED ([ID] ASC)
);

